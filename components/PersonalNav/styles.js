import styled from 'styled-components';

const PersonalNavStyles = styled.section`
  nav {
    ul {
      display: flex;
      justify-content: flex-end;
    }
    li {
      margin-left: 1.2rem;
      font-size: 1.6rem;
    }
  }
`;

export default PersonalNavStyles;
